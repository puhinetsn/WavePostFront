export interface Address {
    departmentId: string;
    departmentAdress: string;
}
  
export interface AssignmentsInfo {
    id: number;
    taskName: string;
    worker: string;
    duration: number;
    payment: number;
}

